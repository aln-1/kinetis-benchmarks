version: '3'

rpc:
  listen: 'tcp://127.0.0.1:6001'

server:
  command: 'php /app/app.php'
  relay: pipes

http:
  address: '0.0.0.0:8080'
  pool:
    num_workers: 2

logs:
  mode: production
  level: warn
